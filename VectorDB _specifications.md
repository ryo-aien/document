# ベクトルデータベース設計仕様書

**プロジェクト名:** 次世代ECサイト 商品検索エンジン
**対象コレクション:** `products_v1`
**作成日:** 2026年02月27日
**使用DB:** Qdrant / Pinecone / Milvus (想定)

## 1. コレクション基本設定 (Collection Config)
MySQLの「テーブル定義」に相当する、データの器そのものの設定です。

| 項目 | 設定値 | 説明 |
| :--- | :--- | :--- |
| **コレクション名** | `products_v1` | テーブル名に相当。 |
| **ベクトル次元数** | `1536` | OpenAI `text-embedding-3-small` を使用想定。この数値は固定。 |
| **距離尺度 (Metric)** | `Cosine` (コサイン類似度) | 意味の近さを測る計算式。他に `Euclidean`(ユークリッド), `Dot`(内積) がある。 |
| **シャード数** | `2` | データの分散数（パーティション）。データ規模により調整。 |

---

## 2. フィールド・ペイロード定義 (Schema / Payload)
MySQLの「カラム定義」に相当します。
ベクトルDBでは、検索対象の「ベクトル」と、フィルタリングや表示用の「メタデータ（Payload）」を定義します。

### A. コアフィールド
| フィールド名 | データ型 | 必須 | 説明 |
| :--- | :--- | :--- | :--- |
| **id** | `uint64` / `uuid` | YES | **プライマリキー**。商品ID（MySQL側のIDと同期させる）。 |
| **vector** | `float32[]` | YES | **ベクトルデータ**。1536次元の浮動小数点配列。 |

### B. メタデータ (Payload)
フィルタリング（例：「価格1000円以上」かつ「カテゴリ＝家電」で検索）に使用する属性情報です。多くのベクトルDBではJSON形式で格納されますが、仕様として以下を定義します。

| キー名 (Key) | データ型 | インデックス | 説明・用途 |
| :--- | :--- | :--- | :--- |
| `product_name` | String | - | 商品名。検索結果の表示用。 |
| `category_id` | Integer | **Keyword Index** | カテゴリ絞り込み用（例: `WHERE category_id = 10`）。MySQLのB-treeインデックスに近い役割。 |
| `price` | Float | **Range Index** | 価格帯フィルタ用（例: `WHERE price >= 1000`）。数値範囲検索に最適化。 |
| `tags` | List[String] | **Keyword Index** | タグ検索用（例: `["Sale", "New"]`）。配列内の要素一致に使用。 |
| `is_active` | Boolean | **Keyword Index** | 論理削除用フラグ。`true` の商品のみ検索対象にする。 |
| `created_at` | String (ISO8601) | - | データ登録日。 |

> **注記:** 「インデックス」列に記載がある項目は、ベクトル検索の前に高速にフィルタリングを行うために、DB側で明示的にインデックスを作成する必要があります。

---

## 3. インデックス設定 (Vector Index Config)
MySQLのインデックス設定より複雑で、**「検索精度」と「速度」のトレードオフ**を決定する設定です。

| 項目 | 設定値 | 説明 |
| :--- | :--- | :--- |
| **インデックスタイプ** | `HNSW` | 現在の主流。高速かつ高精度な近似最近傍探索が可能。 |
| **M (Edges)** | `16` | グラフの各ノードが持つエッジの数。増やすと精度が上がるが、メモリ消費とインサート時間が増える。 |
| **ef_construct** | `100` | インデックス構築時の探索深さ。高いほど構築は遅いが検索精度は上がる。 |

---

## 4. データサンプル (JSON形式)
実際にDBに格納される1レコード（Point）のイメージです。

```json
{
  "id": 10045,
  "vector": [0.012, -0.234, 0.881, ... (合計1536個の数値) ...],
  "payload": {
    "product_name": "ノイズキャンセリング対応ワイヤレスイヤホン Pro",
    "category_id": 501,
    "price": 24800,
    "tags": ["audio", "wireless", "noise-canceling"],
    "is_active": true,
    "created_at": "2026-02-27T10:00:00Z"
  }
}
```

---

## 5. 検索クエリ設計 (Search Interface)
アプリケーション側からどのようなリクエストを受け付けるかの定義です。

**ユースケース: 「予算3万円以内で、高性能なイヤホンが欲しい」と検索**

1.  **入力テキスト:** 「高性能なイヤホン」 → ベクトル化 → `[0.05, ...]`
2.  **フィルタ条件:**
    *   `price` <= 30000
    *   `category_id` IN [501, 502] (オーディオ機器)
    *   `is_active` = true
3.  **SQL的なイメージ:**
    ```sql
    SELECT * FROM products_v1
    WHERE price <= 30000 AND category_id IN (501, 502) AND is_active = true
    ORDER BY CosineSimilarity(vector, InputVector) DESC
    LIMIT 10;
    ```
